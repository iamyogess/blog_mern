let express,dotenv;_c08‍.w("express",[["default",["express"],function(v){express=v}]]);_c08‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();

const app = express();
//middlewares
app.use(express.json());

const PORT = process.env.PORT || 5000;

app.get("/", (req, res) => {
  res.send("hello");
});

app.listen(PORT, () => {
  console.log(`Server started at http://localhost:${port}/`);
});
