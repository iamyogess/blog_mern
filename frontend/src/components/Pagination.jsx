import React from "react";
import { usePagination, DOTS } from "../hooks/userPagination";

const Pagination = ({ siblingCount = 1, currentPage, totalPageCount ,onPageChange}) => {
  return <div>Pagination</div>;
};

export default Pagination;
